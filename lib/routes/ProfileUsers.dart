import 'package:flutter/material.dart';

class ProfileUser extends StatelessWidget {
  static const String routeName = '/telas/Login';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
